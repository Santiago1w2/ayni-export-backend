import{Router}from"express";import{UserRole}from"@prisma/client";import{removeDocument,updateDocument}from"../controllers/requirement-forms.controller";import{requireAuth}from"../middleware/auth.middleware";import{requireRole}from"../middleware/role.middleware";const r=Router();r.use(requireAuth,requireRole(UserRole.IMPORTER));r.patch("/:id",updateDocument);r.delete("/:id",removeDocument);export default r;

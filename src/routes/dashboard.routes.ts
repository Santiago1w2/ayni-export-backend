import{UserRole}from"@prisma/client";import{Router}from"express";import{exporter,importer}from"../controllers/dashboard.controller";import{requireAuth}from"../middleware/auth.middleware";import{requireRole}from"../middleware/role.middleware";import{requireVerifiedEmail}from"../middleware/verified-email.middleware";const r=Router();r.get("/exporter",requireAuth,requireVerifiedEmail,requireRole(UserRole.EXPORTER),exporter);r.get("/importer",requireAuth,requireVerifiedEmail,requireRole(UserRole.IMPORTER),importer);export default r;

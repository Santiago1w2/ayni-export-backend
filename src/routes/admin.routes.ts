import{Router}from"express";import * as c from"../controllers/admin.controller";import{requireAdmin}from"../middleware/admin.middleware";import{requireAuth}from"../middleware/auth.middleware";const r=Router();r.use(requireAuth,requireAdmin);r.get("/stats",c.stats);r.get("/users",c.users);r.get("/companies",c.companies);r.get("/companies/:id",c.company);r.post("/companies/:id/verify",c.verify);r.post("/companies/:id/reject-verification",c.reject);r.post("/users/:id/block",c.block);r.post("/users/:id/suspend",c.suspend);r.post("/users/:id/unblock",c.unblock);r.get("/audit-logs",c.logs);export default r;


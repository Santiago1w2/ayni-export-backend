import{Router}from"express";import{UserRole}from"@prisma/client";import * as c from"../controllers/requirement-forms.controller";import{requireAuth}from"../middleware/auth.middleware";import{requireRole}from"../middleware/role.middleware";const r=Router();r.use(requireAuth,requireRole(UserRole.IMPORTER));r.post("/",c.create);r.get("/me",c.mine);r.get("/:id",c.one);r.patch("/:id",c.update);r.delete("/:id",c.remove);r.post("/:id/fields",c.addField);r.post("/:id/documents",c.addDocument);export default r;
